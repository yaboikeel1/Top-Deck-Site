const form=document.getElementById("bookingForm");const statusEl=document.getElementById("formStatus");function encode(data){return new URLSearchParams(data).toString()}if(form){form.addEventListener("submit",async e=>{e.preventDefault();const fd=new FormData(form);const data=Object.fromEntries(fd.entries());if(!data.name||!data.email||!data.type||!data.message){statusEl.textContent="Please fill out name, email, booking type, and message.";statusEl.style.color="#ff8a8a";return}const existing=JSON.parse(localStorage.getItem("td_booking_requests")||"[]");existing.unshift({...data,status:"New",submittedAt:new Date().toISOString()});localStorage.setItem("td_booking_requests",JSON.stringify(existing));try{await fetch("/",{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body:encode(fd)});statusEl.textContent="Request submitted.";statusEl.style.color="#6ee7a8"}catch(err){statusEl.textContent="Saved locally. Deploy on Netlify to receive live submissions.";statusEl.style.color="#6ee7a8"}form.reset()})}